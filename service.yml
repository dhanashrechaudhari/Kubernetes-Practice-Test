apiVersion: v1
kind: Service
metadata:
  name: task-mysvc
spec:
  selector:
    app: task-webapp
  type: NodePort
  ports:
    - protocol: TCP
      port: 80 # service port
      targetPort: 80 # pod port

 